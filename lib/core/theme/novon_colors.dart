import 'package:flutter/material.dart';

class NovonColors {
  NovonColors._();

  // Primary palette — deep indigo with electric accents
  static Color primary = Color(0xFF6C63FF);
  static Color primaryLight = Color(0xFF9D97FF);
  static Color primaryDark = Color(0xFF4A42DB);

  // Accent — warm amber for CTAs and highlights
  static Color accent = Color(0xFFFFB74D);
  static Color accentLight = Color(0xFFFFD180);

  // Surface colors — dark mode first
  static Color background = Color(0xFF0D0D0F);
  static Color surface = Color(0xFF16161A);
  static Color surfaceVariant = Color(0xFF1E1E24);
  static Color surfaceElevated = Color(0xFF242430);
  static Color surfaceOverlay = Color(0xFF2A2A36);

  // AMOLED
  static Color amoledBackground = Color(0xFF000000);
  static Color amoledSurface = Color(0xFF0A0A0A);

  // Sepia
  static Color sepiaBackground = Color(0xFFF4ECD8);
  static Color sepiaSurface = Color(0xFFEDE4CB);
  static Color sepiaText = Color(0xFF5B4636);

  // Light
  static Color lightBackground = Color(0xFFF8F8FC);
  static Color lightSurface = Color(0xFFFFFFFF);
  static Color lightSurfaceVariant = Color(0xFFF0F0F6);

  // Text
  static Color textPrimary = Color(0xFFF0F0F5);
  static Color textSecondary = Color(0xFF9090A0);
  static Color textTertiary = Color(0xFF5A5A6E);
  static Color textOnPrimary = Color(0xFFFFFFFF);

  // Status
  static Color success = Color(0xFF4CAF50);
  static Color warning = Color(0xFFFF9800);
  static Color error = Color(0xFFEF5350);
  static Color info = Color(0xFF42A5F5);

  // Borders & dividers
  static Color border = Color(0xFF2A2A36);
  static Color divider = Color(0xFF1E1E28);

  // Shimmer
  static Color shimmerBase = Color(0xFF1E1E24);
  static Color shimmerHighlight = Color(0xFF2A2A36);

  // Novel status
  static Color ongoing = Color(0xFF42A5F5);
  static Color completed = Color(0xFF66BB6A);
  static Color hiatus = Color(0xFFFFCA28);
  static Color dropped = Color(0xFFEF5350);
}
